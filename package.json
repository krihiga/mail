{
  "name": "gmail-api-vercel",
  "version": "1.0.0",
  "description": "Send emails with Gmail API using Vercel",
  "scripts": {
    "start": "node sendMail.js",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "formidable": "^3.5.2",
    "googleapis": "^118.0.0",
    "nodemailer": "^6.9.3"
  },
  "devDependencies": {
    "@vercel/node": "^2.11.0"
  }
}
